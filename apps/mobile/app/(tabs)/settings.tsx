/**
 * Settings tab (technical specification section 8).
 *
 * MTS-003 ships only the approved placeholder shell; the Settings product
 * screen belongs to later tickets.
 */
import { PlaceholderScreen } from "../../src/components/PlaceholderScreen";

export default function SettingsTab() {
  return <PlaceholderScreen titleKey="tabs.settings" bodyKey="placeholders.settings" />;
}
