/**
 * Progress tab (technical specification section 8).
 *
 * MTS-003 ships only the approved placeholder shell; the Progress product
 * screen belongs to later tickets.
 */
import { PlaceholderScreen } from "../../src/components/PlaceholderScreen";

export default function ProgressTab() {
  return <PlaceholderScreen titleKey="tabs.progress" bodyKey="placeholders.progress" />;
}
