/**
 * AI Planner tab (technical specification section 8).
 *
 * MTS-003 ships only the approved placeholder shell; the AI Planner product
 * screen belongs to later tickets.
 */
import { PlaceholderScreen } from "../../src/components/PlaceholderScreen";

export default function AiPlannerTab() {
  return <PlaceholderScreen titleKey="tabs.aiPlanner" bodyKey="placeholders.aiPlanner" />;
}
