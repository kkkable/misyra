name: CI

on:
  pull_request:

permissions:
  contents: write

concurrency:
  group: mts056-green-format-${{ github.ref }}
  cancel-in-progress: true

jobs:
  format-green-implementation:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout MTS-056 head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Install pinned pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.34.5
          run_install: false

      - name: Set up pinned Node
        uses: actions/setup-node@v4
        with:
          node-version: 24.19.0
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install from lockfile
        run: pnpm install --frozen-lockfile

      - name: Format GREEN implementation and restore CI
        run: |
          pnpm exec prettier --write \
            apps/api/src/ai-difficulty-classification.ts \
            --config packages/config/prettier.config.mjs
          git fetch origin main
          git checkout origin/main -- .github/workflows/ci.yml
          git config user.name "Misyra CI Helper"
          git config user.email "actions@users.noreply.github.com"
          git add apps/api/src/ai-difficulty-classification.ts \
            .github/workflows/ci.yml
          if git diff --cached --quiet; then
            echo "No formatting changes required."
            exit 1
          fi
          git commit -m "ci: format MTS-056 GREEN implementation"
          git push origin HEAD:${{ github.head_ref }}
