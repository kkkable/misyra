// Representative non-tab root route group layout (test fixture only,
// non-routable). Stands in for approved future full-screen modal routes that
// live outside the permanent (tabs) group.
export default null;
