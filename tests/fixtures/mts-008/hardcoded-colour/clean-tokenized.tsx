/**
 * Clean fixture for the MTS-008 hard-coded colour guard: product source
 * that references design tokens only. Must produce zero violations.
 */
import { themes } from "@misyra/design-tokens";

export const titleColor = themes.light.textPrimary;
export const screenBackground = themes.dark.surface;
export const buttonBackground = themes.light.primary;
